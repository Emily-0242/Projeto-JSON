<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Usuário</title>
    <%- include("../partials/cabecalho.ejs") %>
</head>
<body>
    
    <div class="container">
        <form action="/usuarios/editar" method="post">
            <input type="hidden" name="id_usuario" value="<%= usuario._id %>">
            <div>
                <label for="inp_nome" class="form-label">Nome</label> 
                <input class="form-control" value="<%= usuario.nome %>" type="text" required placeholder="Seu nome" name="inp_nome">
            </div>
            <div>
                <label for="inp_email" class="form-label">E-mail</label>
                <input class="form-control" value="<%= usuario.email %>" type="text" required placeholder="Seu E-mail" name="inp_email">
            </div>
            <div>
                <label for="inp_cpf" class="form-label">CPF</label>
                <input class="form-control" value="<%= usuario.cpf %>" type="text" required placeholder="Seu CPF" name="inp_cpf">
            </div>
            <div>
                <a href="/usuarios/cadastrar/<%= usuario.cpf %>">
                    <button class="btn btn-warning">Editar</button>
                </a>
                <input class="btn btn-success" type="submit" value="Salvar">
                
            </div>
        </form>
    </div>
    <%- include("../partials/rodape.ejs") %>
</body>
</html>